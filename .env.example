# Airtable — see docs/airtable-spec-for-ai.md.
# Required for `next dev`/`next build`: the /survey contribute form writes
# submissions straight into the Pending Queue table via this API key
# (lib/submission-store.ts). Also used by the sync script (scripts/sync-airtable.mjs).
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=apps8rBIORsmE7ij8
AIRTABLE_CANON_TABLE_ID=Canon
AIRTABLE_PENDING_QUEUE_TABLE_ID=Pending Queue
